# Beamable API

The Beamable API is the HTTP interface the Beamable SDKs are built on. You can call it directly from any language or runtime that can make HTTP requests.

Nearly every route requires credentials, so start with [Authenticating the API](./authenticating.md). The [OpenAPI specification](./oapi.md) is the complete route reference.
