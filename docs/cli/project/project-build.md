# Project build

## Overview

The `beam project build` command builds your Beamable microservices locally. You can build specific services or all services in your project with various configuration options.

## Prerequisites

You need:
- A configured Beamable project with microservices
- Docker installed and running
- .NET SDK installed

## Steps

1. Navigate to your Beamable project directory
2. Run the build command: